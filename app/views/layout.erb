<!doctype html>
<html>
  <head>
    <nav class="navbar">
      <span class="navbar-span">

        <%if Helpers.is_logged_in?(session)%>
          Current User:
          <% @user = Helpers.current_user(session) %>
          <%=" #{@user.username}"%>
          <br> |
          <a href="/users/<%="#{@user.id}"%>">Your Profile</a> |
          <a href="/tweets/new">Post New Tweet</a> |
          <a href="/">Home</a> |
          <br>
          <a href="/users/logout">Logout</a>
        <%else%>
          <h3><a href="/users/login"><button class="button-green">Login</button></a></h3>
          <h3><a href="/users/signup"><button class="button-green">Sign Up</button></a></h3>
        <%end%>
        </span>
    </nav>
    <title>Fwitter</title>
  </head>
  <body>
<!--
    <div class="container">
      <h1>Welcome to Fwitter!</h1>
      <h2>This is located in layout.erb!</h2>
      <h2>Don't forget to <strong>yield</strong> your views!</h2>
    </div> -->
    <%= yield %>
  </body>
</html>
